/**
 * config.js — CETSure global configuration
 * Update API_BASE to your Railway deployment URL before going live.
 * Firebase config will be filled in manually.
 */

// ── Backend API ─────────────────────────────────────────────
const API_BASE = "https://cetsure-production.up.railway.app";

// ── Firebase (fill in from Firebase Console → Project Settings) ──
const FIREBASE_CONFIG = {
  apiKey: "",
  authDomain: "",
  projectId: "",
  storageBucket: "",
  messagingSenderId: "",
  appId: "",
};
